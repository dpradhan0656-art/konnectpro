node_modules/
.env
.env.local
.env.*.local
dist/
.vercel/
*.log
android/.gradle-local/

# Supabase CLI local cache (never commit)
supabase/.temp/

# Android release signing (never commit)
*.keystore
*.jks

# Expert Expo — local EAS archive inspect output (can be multi‑GB; never commit)
expert-expo-app/_eas_inspect_out/
expert-expo-app/_eas_inspect_archive*/
expert-expo-app/dist-expo-verify-native/

# Local EAS env preview dumps — debug only, never commit
expert-expo-app/_eas_env_*.json

# Local asset backups created before regeneration — never commit
public/images/*.bak.png

